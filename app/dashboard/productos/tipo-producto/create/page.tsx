'use client';

import TipoProductoForm from '../componentes/TipoProductoForm';

/** [3.3] Antes tenía su propia copia del formulario. Ahora usa el form único. */
export default function Page() {
  return <TipoProductoForm />;
}
